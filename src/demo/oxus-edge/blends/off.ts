import type { BlendMode } from "./types";

const blend: BlendMode = {
  id: "0",
  label: "Off",
  group: "Basic",
  description: "Hard edges",
  css: `/* Mode 0 — no extra styles, sections use their Tailwind bg classes as-is */`,
};

export default blend;
